spring:
  application:
    name: admin
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: admin
        prefer-ip-address: true
      config:
        profile-separator: '-'
        format: yaml


