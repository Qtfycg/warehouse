spring:
  application:
    name: user
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: user
        prefer-ip-address: true
      config:
        profile-separator: '-'
        format: yaml



