spring:
  application:
    name: file
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: file
        prefer-ip-address: true
      config:
        profile-separator: '-'
        format: yaml


