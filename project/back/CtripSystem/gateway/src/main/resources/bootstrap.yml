spring:
  application:
    name: gateway
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true
        hostname: localhost
      config:
        profile-separator: '-'
        format: yaml




