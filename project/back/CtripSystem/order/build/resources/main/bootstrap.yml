spring:
  application:
    name: order
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: order
        prefer-ip-address: true
      config:
        profile-separator: '-'
        format: yaml


