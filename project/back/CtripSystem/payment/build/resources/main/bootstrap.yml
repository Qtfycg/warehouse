spring:
  application:
    name: payment
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: payment
        prefer-ip-address: true
      config:
        profile-separator: '-'
        format: yaml